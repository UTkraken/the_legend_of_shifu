/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stock_v2 : MonoBehaviour
{
    
    public static List<Card> copyDeck = new List<Card>();

    //private int deckSize = copyDeck.Count;
    // Start is called before the first frame update
    void Start()
    {
        copyDeck.Add(new Card(1, "lucas"));
        copyDeck.Add(new Card(2, "arthur"));
        copyDeck.Add(new Card(3, "yvan"));
        int deckSize = copyDeck.Count;
    }


    void draw()
    {
        
    }
    
    void discard(Card card)
    {
        
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
*/
